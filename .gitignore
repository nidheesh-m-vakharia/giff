# Rust
target/

# Node / SvelteKit
node_modules/
.svelte-kit/
build/
.vercel/
dist/

# Embedded dashboard assets — populated by build.rs (stub) or CI (real SvelteKit
# build). Stays out of git but ships in the published .crate via `include` in
# crates/giff-cli/Cargo.toml.
crates/giff-cli/dashboard-dist/

# Per-user / local
config/
.env
.env.local
.DS_Store

# IDE
.idea/
.vscode/
