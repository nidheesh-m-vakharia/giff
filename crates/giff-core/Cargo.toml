[package]
name = "giff-core"
version = "0.2.0"
edition = "2021"
description = "Pure-Rust core types and algorithms for giffstack — stack data model, validation, and traversal."
license = "MIT"
repository = "https://github.com/nidheesh-m-vakharia/giff"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"
toml = "0.8"
